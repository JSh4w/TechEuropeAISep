# Copy to .env (scripts/setup.sh does this) and fill in. Never commit .env.
# Get the values from Josh over a private channel.

# LLMs via Pydantic AI Gateway, e.g. Agent("gateway/anthropic:claude-sonnet-4-6")
PYDANTIC_AI_GATEWAY_API_KEY=
# Jev classification model, e.g. Agent("typesafe:jev-latest", output_type=...)
TYPESAFE_API_KEY=
# Optional: Logfire tracing
LOGFIRE_TOKEN=

TEMPORAL_ADDRESS=localhost:7233
TEMPORAL_NAMESPACE=default
